services:
  vllm:
    image: scitrera/dgx-spark-vllm:0.17.0-t4
    container_name: sparkdeck-vllm-qwen3.5-27b
    ports:
      - "8000:8000"
    volumes:
      - huggingface-cache:/root/.cache/huggingface
    ipc: host
    command:
      - python3
      - -m
      - vllm.entrypoints.openai.api_server
      - --model
      - Qwen/Qwen3.5-27B-FP8
      - --port
      - "8000"
      - --max-model-len
      - "262144"
      - --enable-auto-tool-choice
      - --tool-call-parser
      - qwen3_xml
      - --enable-prefix-caching
      - --gpu-memory-utilization
      - "0.7"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped

volumes:
  huggingface-cache:
