services:
  flowise:
    image: flowiseai/flowise:latest
    container_name: sparkforge-flowise
    ports:
      - "3002:3000"
    volumes:
      - flowise-data:/root/.flowise
    restart: unless-stopped

volumes:
  flowise-data:
