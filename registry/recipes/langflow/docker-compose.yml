services:
  langflow:
    image: langflowai/langflow:latest
    container_name: sparkforge-langflow
    ports:
      - "7860:7860"
    volumes:
      - langflow-data:/app/langflow
    restart: unless-stopped

volumes:
  langflow-data:
