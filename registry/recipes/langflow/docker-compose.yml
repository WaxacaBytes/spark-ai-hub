services:
  langflow:
    image: langflowai/langflow:latest
    container_name: sparkdeck-langflow
    ports:
      - "7860:7860"
    volumes:
      - langflow-data:/app/langflow
    restart: unless-stopped

volumes:
  langflow-data:
