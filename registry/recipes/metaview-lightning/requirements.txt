# MetaView Lightning runtime deps (aarch64 / CUDA 13).
#
# torch / torchvision / torchaudio are installed separately from the cu130
# wheel index in the Dockerfile (the PyPI torchvision would pull a CPU build and
# clobber the CUDA one), so they are intentionally NOT listed here.
#
# flash_attn / xformers / sageattention / gsplat are deliberately omitted: every
# attention path in diffsynth + src/MetaView_dit falls back to torch SDPA, and
# gsplat is only used by the 3DGS export path this app never calls. We also set
# DIFFSYNTH_ATTENTION_IMPLEMENTATION=torch at runtime to force the SDPA path.
transformers>=4.51.0
diffusers>=0.32.0
matplotlib
accelerate
peft
safetensors
einops
sentencepiece
protobuf
ftfy
imageio
imageio[ffmpeg]
pandas
modelscope
omegaconf
addict
opencv-python-headless
evo
numpy<2
gradio==5.49.1
huggingface_hub[hf_xet]
